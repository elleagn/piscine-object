#pragma once

struct Position
{
    int x;
    int y;

    Position();
    ~Position();
};

struct Statistic {
    int level;
    int exp;
    Statistic();
    ~Statistic();
};

class Worker {
    pri
}
